{
  "name": "fortune-wheel-booth",
  "scripts": {
    "postinstall": "mops install",
    "build": "pnpm run -r build",
    "prebuild": "pnpm run -r prebuild",
    "pretest": "pnpm run -r prebuild",
    "start": "pnpm run -r start",
    "test": "pnpm run -r test",
    "prepare": "husky"
  },
  "type": "module",
  "engines": {
    "node": "^20",
    "pnpm": "^9",
    "npm": "please use pnpm"
  }
}
